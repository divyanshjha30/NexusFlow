# ── Secrets & environment ──
# .env.dev holds real floci resource IDs — never commit it.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.jks

# ── Java / Maven ──
target/
build/
out/
*.class
*.war
*.ear
hs_err_pid*
replay_pid*
.mvn/timing.properties

# Maven wrapper is committed; negations must follow the *.jar rule to win.
*.jar
!.mvn/wrapper/maven-wrapper.jar

# ── Node / frontend ──
node_modules/
dist/
dist-ssr/
.vite/
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.eslintcache

# ── IDE & editors ──
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
.classpath
.project
.settings/
nbproject/

# ── OS ──
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Logs & local scratch ──
*.log
logs/
tmp/
temp/

# ── Docker / infra local state ──
docker/data/
docker/volumes/
*.tar
